---
title: 'Listar Faturas'
description: 'Retorna uma lista de todas as faturas'
openapi: 'GET /api/v1/invoices'
---

Retorna uma lista paginada de todas as faturas. Pode ser filtrada por cliente ou status.

## Endpoint

```bash cURL
curl -G https://api.upag.io/v1/invoices \
  -H "Authorization: Bearer {token}" \
  -d page=1 \
  -d limit=10 \
  -d customer=cus_ahwDXrgYvur89iPs
```

## Parâmetros

<ParamField query="limit" type="integer">
  Limita o número de faturas retornadas. Padrão: `10`, máximo: `100`.
</ParamField>

<ParamField query="page" type="integer">
  Número da página a ser retornada. Padrão: `1`.
</ParamField>

<ParamField query="customer" type="string">
  ID do cliente para filtrar faturas de um cliente específico.
</ParamField>

<ParamField query="status" type="string">
  Filtra faturas por status: `draft`, `open`, `paid`, `uncollectible`, `void`.
</ParamField>

## Resposta

```json Response
{
  "data": [
    {
      "id": "inv_abc123xyz",
      "livemode": false,
      "customer": "cus_ahwDXrgYvur89iPs",
      "paymentMethod": "pm_abc123xyz",
      "status": "open",
      "currency": "BRL",
      "createdAt": 1731622178441,
      "updatedAt": 1731622178441,
      "canceledAt": null,
      "items": [
        {
          "id": "ii_abc123xyz",
          "product": "prod_xyz789abc",
          "price": "price_def456ghi",
          "name": "Plano Pro",
          "amount": 9900,
          "quantity": 1
        }
      ]
    }
  ],
  "total": 1
}
```
